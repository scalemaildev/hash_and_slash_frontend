local function _js_listener(self, message_id, message)
	if message_id == hash("join_room") then
		msg.post("#colyseus", "join_room", { room_id = message.room_id })
	end
end

function init(self)
	if jstodef then
		jstodef.add_listener(_js_listener)
	end
end